{
  "url": "https://example.com",
  "recording_type": "task_tutorial",
  "goal": "<one-line description of what the user wants to demonstrate>",
  "auth": {
    "required": false,
    "evidence": "The dry-run reached the target form without a login redirect."
  },
  "pages": [
    {
      "url": "https://example.com",
      "purpose": "Landing page entry point",
      "headings": ["<H1 text>", "<key subheading>"],
      "actions": [
        {
          "label": "<primary CTA label>",
          "role": "link",
          "selector": "page.getByRole('link', { name: /<pattern>/i })",
          "result": "<what happens when clicked>",
          "primary": true
        }
      ],
      "fields": [],
      "buttons": [],
      "risks": ["<e.g. Cookie banner can block the first CTA>"]
    },
    {
      "url": "https://example.com/<next-page>",
      "purpose": "<purpose of this page in the flow>",
      "headings": ["<H1>", "<H2>"],
      "actions": [
        {
          "label": "<action label>",
          "role": "button",
          "selector": "page.getByRole('button', { name: /<pattern>/i })",
          "result": "<observed result>",
          "primary": true
        }
      ],
      "fields": [
        {
          "label": "<field label from DOM>",
          "selector": "page.getByLabel(/<pattern>/i)",
          "required": true,
          "type": "text",
          "placeholder": "<observed placeholder>",
          "valid_demo_value": "<value that passes validation>",
          "validation_errors": ["<exact error text you saw>"],
          "evidence": "<what you did that surfaced the error>"
        }
      ],
      "buttons": [
        {
          "label": "<submit button text>",
          "selector": "page.getByRole('button', { name: /<pattern>/i })",
          "initial_state": "disabled",
          "enabled_when": ["<precondition 1>", "<precondition 2>"],
          "primary": true
        }
      ],
      "success_state": {
        "selector": "page.getByText(/<success text or URL pattern>/i)",
        "evidence": "<what appeared after a valid submission>"
      },
      "risks": ["<anything that could publish, charge, email, etc. for real>"]
    }
  ],
  "blockers": ["<e.g. Cookie banner blocks first CTA>"],
  "sensitive_actions": ["<anything that must not run against production data>"],
  "readiness": {
    "entry_action_known": true,
    "required_fields_known": true,
    "valid_demo_values_known": true,
    "primary_buttons_known": true,
    "success_state_known": true,
    "blockers_reviewed": true,
    "sensitive_actions_reviewed": true
  }
}
